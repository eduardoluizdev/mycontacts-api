apiVersion: batch/v1
kind: Job
metadata:
  name: my-contacts
spec:
  template:
    metadata:
      name: my-contacts-pod
    spec:
      containers:
      - name: mycontacts
        image: mycontacts
        imagePullPolicy: Never
      restartPolicy: Never
